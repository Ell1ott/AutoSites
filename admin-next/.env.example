# Pi / local backend (FastAPI). Default matches backend dev port (:8888).
NEXT_PUBLIC_PI_URL=http://localhost:8888

# Optional; omit in local dev if the backend has no BACKEND_AUTH_TOKEN set.
# NEXT_PUBLIC_BACKEND_TOKEN=
