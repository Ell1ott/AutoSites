# Copy to .env.local (gitignored). Used by scripts/vercel-deploy.ts.
# Create a token at https://vercel.com/account/tokens
VERCEL_TOKEN=
# Only needed if the projects live in a Vercel team (not your personal account):
VERCEL_TEAM_ID=
# or VERCEL_TEAM_SLUG=
